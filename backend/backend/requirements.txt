# Agentic Hybrid RAG System — Python Dependencies
# Install with: pip install -r requirements.txt

# Database
sqlalchemy==2.0.36
psycopg2-binary>=2.9.9
pymssql>=2.3.0
pyodbc
pymongo>=4.7.0

# Vector Database
pinecone==6.0.0
# sentence-transformers removed — embeddings now use Gemini API (zero RAM overhead)

# Graph
networkx==3.4.2
neo4j==5.26.0

# LLM
google-generativeai==0.8.3

# Agentic Orchestration
langgraph>=0.2.56
langchain-core>=0.3.0,<0.4
# langchain-pinecone and langchain-huggingface are only needed for offline
# data ingestion (Data_ingestion__to_vectorDB.py) — not required at runtime.

# FastAPI / API Layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Utilities
numpy==1.26.4
pandas==2.2.3
tqdm==4.67.1
python-dotenv==1.0.1
sqlparse>=0.5.0
requests>=2.31.0
